# Example environment configuration
# Copy this file to .env.local for development
# For production, these values should be set as GitHub Secrets or environment variables

# API Configuration (REQUIRED)
# ⚠️  REQUIRED: This must be set for the application to work
VITE_API_BASE_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
VITE_API_KEY=your-optional-api-key
VITE_ALLOWED_ORIGINS=https://uvaheesara.uvaarchery.lk,https://localhost:3000,http://127.0.0.1:3000
VITE_ENVIRONMENT=development

# Security Configuration (optional)
VITE_ENABLE_API_ENCRYPTION=false
VITE_API_TIMEOUT=10000

# Asgardeo Authentication Configuration
# ⚠️  SECURITY: Never commit real credentials to version control
# These are example values - replace with your actual Asgardeo app credentials
VITE_ASGARDEO_CLIENT_ID=your-asgardeo-client-id
VITE_ASGARDEO_BASE_URL=https://api.asgardeo.io/t/your-organization
VITE_ASGARDEO_REDIRECT_URL=https://uvaheesara.uvaarchery.lk

# Production Domain Configuration
VITE_PRODUCTION_DOMAIN=uvaheesara.uvaarchery.lk
